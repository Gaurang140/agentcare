# Build context here is frontend/ (docker-compose.yml build.context), so
# this is the .dockerignore that actually applies to that build - the root
# one only covers a context of ".".
node_modules/
.next/
.git
.env*
npm-debug.log*
